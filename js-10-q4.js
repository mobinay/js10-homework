// What is the result of the code below?
const hello = "what's up";
const obj = {
  func() {
    return `${hello} aps?!`;
  },
};
console.log(obj.func()); // answer is?:
// hello(what's up)+aps= what's up aps?!
